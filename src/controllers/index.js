const login = require('./login');
const createUser = require('./createUser');
const getUsers = require('./getUsers');
const getByUserId = require('./getByUserId');

module.exports = {
  login, createUser, getUsers, getByUserId,
};